#!/bin/bash

set -ex

# Run pylint on all the python
$( dirname $0 )/pylint

# Run the python tests
$( dirname $0 )/pytest

# Generate any generated code
$( dirname $0 )/code_gen

cargo build
cargo test
