#!/bin/bash

set -ex

# Download the test roms
$( dirname $0 )/download_test_roms

CHANGES=$( git diff )
if [ "$CHANGES" != "" ]; then
    echo "Untracked Changes"
    exit 1;
fi;

cargo build --release
cargo test --release
