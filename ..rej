--- pico/src/panic.rs
+++ pico/src/panic.rs
@@ -10,10 +10,10 @@ use core::sync::atomic::{self, Ordering};
 fn panic(info: &PanicInfo) -> ! {
     let graphics = Graphics::new();
 
-    graphics.set_pen(crate::graphics::Color::rgb(30, 12, 0));
+    graphics.set_pen(crate::graphics::Color::rgb(219, 58, 4));
     graphics.clear();
 
-    graphics.set_pen(crate::graphics::Color::rgb(0, 0, 0));
+    graphics.set_pen(crate::graphics::Color::rgb(255, 255, 255));
 
     let message = if let Some(loc) = info.location() {
         let file = loc.file().replace("/", "/ ");
